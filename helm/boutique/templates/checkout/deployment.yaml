{{- if .Values.checkout.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "boutique.fullname" . }}-checkout
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "boutique.checkoutSelectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.checkout.replicaCount }}
  selector:
    matchLabels:
      {{- include "boutique.checkoutSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "boutique.checkoutSelectorLabels" . | nindent 8 }}
        version: {{ .Values.checkout.image.tag | quote }}
    spec:
      containers:
        - name: checkout
          image: "{{ .Values.checkout.image.repository }}:{{ .Values.checkout.image.tag }}"
          imagePullPolicy: {{ .Values.checkout.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.checkout.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.checkout.port | quote }}
            - name: APP_VERSION
              value: {{ .Values.checkout.image.tag | quote }}
          resources:
            {{- toYaml .Values.checkout.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
{{- end }}
